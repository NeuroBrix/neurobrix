# FIXTURE, not a detected machine. A real V100-SXM2-32GB with the host RAM declared at
# 24 576 MB instead of this rack's 257 530.
#
# WHY IT EXISTS. `layer_streaming` is unreachable on this rack: measured at rungs 4096,
# 8192 and 12288 under the rack's own profile, the answer is `lazy_sequential` every time,
# because 251 GB of host makes offloading always viable. The class-1 MoE models reached
# layer_streaming only under the Mac's UNIFIED profile — and executing that plan on a
# discrete 16 GB V100 dies early (`GPU malloc failed for 30993538048 bytes`), because the
# plan is taken for an 18 GB unified device and run on a 16 GB discrete one.
#
# This profile removes the mismatch: a REAL device this rack has, with a host small enough
# that offload stops being free. The plan and the execution then describe the same machine,
# which is the only way the streamed run can be judged on CUDA.
#
# Gitignored, like every generated profile. Nothing here is doctrine.
# Hardware Profile: 1 x Tesla V100-SXM2-32GB
# Auto-generated by NeuroBrix hardware detection
# Regenerate: delete this file and run neurobrix without --hardware

id: v100-32g-tighthost
vendor: dell
preferred_dtype: float16
summary:
  total_gpus: 1
  total_vram_gb: 32.0
  total_ram_gb: 24.0
  topology: Single-GPU
cpu:
  model: Intel(R) Xeon(R) Gold 6230 CPU @ 2.10GHz
  cores: 40
  threads: 80
  ram_mb: 24576
  architecture: x86_64
  features:
  - fma
  - sse4_1
  - sse4_2
  - avx
  - f16c
  - avx2
  - avx512f
  - avx512_vnni
devices:
- index: 0
  brand: nvidia
  model: Tesla V100-SXM2-32GB
  memory_mb: 32768
  compute_capability: '7.0'
  supports_dtypes:
  - float32
  - float16
  architecture: volta
  pcie_version: '3.0'
  physical_index: 3
interconnect:
  groups: []
pcie_fallback:
  version: '3.0'
  lanes: 16
  bandwidth_gbps: 32
notes: '1 x Tesla V100-SXM2-32GB

  GPU brand: nvidia

  Architecture: volta

  CPU: Intel(R) Xeon(R) Gold 6230 CPU @ 2.10GHz (40 cores, 251.5 GB RAM)

  System vendor: dell

  Auto-generated by NeuroBrix hardware detection.'
